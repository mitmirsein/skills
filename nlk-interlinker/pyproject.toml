[project]
name = "nlk-interlinker"
version = "1.0.0"
description = "국립중앙도서관 인터링킹 LOD 정보 제공 API 스킬"
requires-python = ">=3.11"
dependencies = [
    "httpx>=0.28.1",
    "beautifulsoup4>=4.12.0",
    "pyyaml>=6.0.1",
]

[tool.uv]
# 추가 설정 없음
